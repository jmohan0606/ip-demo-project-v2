// Chart data shapes shared by the Recharts components.
export type PerformancePoint = {
  period: string;
  revenue: number;
};

export type ProductMixPoint = {
  category: string;
  revenue: number;
};
