cd C:\Users\R757680\ds\workspace\ip-demo-project-v2

# common locations for ingestion state
Get-ChildItem -Recurse -Directory | Where-Object { $_.Name -match "checkpoint|ingestion|runtime|state|\.cache" } | Select-Object FullName

# any json/db that mentions these entities or "checkpoint"
Get-ChildItem -Recurse -Include *.json,*.db,*.sqlite,*.state | Where-Object { $_.FullName -notmatch "node_modules|\.venv|data\\sample|data\\real\\vertices|data\\real\\edges" } | Select-Object FullName

# and grep for the delta/checkpoint state referencing revenue_class
Get-ChildItem -Recurse -Include *.json | Select-String -List "last_processed_row|checkpoint|revenue_class" | Select-Object Path -Unique