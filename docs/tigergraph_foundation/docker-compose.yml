services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile.root
    env_file: ./backend/.env
    volumes:
      - ./runtime:/app/runtime
    ports: ["8000:8000"]
  frontend:
    build: ./frontend
    ports: ["5173:80"]
    depends_on: [backend]
