# ROUND 11 — CHANGED FILES (git-derived: `ddf1172..HEAD`)

Derived from `git diff --name-status ddf1172..HEAD`. Operator-local material
(`data/real/`, `prompts/`, gitignored screenshot/log dirs) is excluded — the
round never touched it. **CONFLICT-RISK** flags files other work-streams or
operator edits commonly touch.

## Work-stream A — taxonomy patch (commit 360b19a)

| File | Change | Notes |
|---|---|---|
| `app/v2/revenue/taxonomy.py` | M | ALTI line (NON_RECURRING, assumption comment); `PRODUCT_GRID_TYPE`; `NonProductGridRowError`; `resolve_path(grid_type=…)` guard. **CONFLICT-RISK** (every round touches it) |
| `scripts/build_real_data.py` | M | non-PRODUCT_TYPE rows filtered BEFORE classification; `nongrid_*` holding lines |
| `scripts/verify_taxonomy.py` | M | A1 constant + counts 13/35; section [4b] ALTI; section [7] 42-path real-hierarchy fixture |

## Work-stream B + C backend (commit d41cda6)

| File | Change | Notes |
|---|---|---|
| `docs/tigergraph_foundation/tigergraph/schema/01_vertices.gsql` | M | `advisor_sid` on commentary_version + anomaly_scan. **CONFLICT-RISK** |
| `docs/tigergraph_foundation/tigergraph/schema/schema_catalog.json` | M | regenerated from DDL |
| `docs/tigergraph_foundation/tigergraph/loading/jobs/load_v2_all.gsql` | M | regenerated |
| `docs/tigergraph_foundation/data/manifest.json` | M | regenerated by the builder (advisor_sid columns; July counts). **CONFLICT-RISK** (build_real_data rewrites it on the client machine) |
| `app/v2/dataset/builder.py` | M | advisor_sid in both preserve_or_create headers. **CONFLICT-RISK** |
| `app/v2/commentary/generation_workflow.py` | M | per-advisor `_run_for_advisor`; `start_generation` async; per-advisor supersede; `--advisor` CLI. **CONFLICT-RISK** |
| `app/v2/anomalies/detection.py` | M | per-advisor scans; `start_scan` async; `--advisor` CLI |
| `app/v2/anomalies/service.py` | M | `scans(advisor_id)` |
| `app/graph/queries/v2.py` | M | advisor-aware resolution in get_commentary/get_anomalies; advisor_id filter in get_commentary_versions/get_anomaly_scans. **CONFLICT-RISK** |
| `docs/tigergraph_foundation/tigergraph/queries/GQ-009/010/018/019*.gsql` | M | mirrors of the above — NEEDS LIVE REINSTALL |
| `docs/tigergraph_foundation/tigergraph/queries/query_catalog.json` | M | advisor_id params; r11 status flags. **CONFLICT-RISK** |
| `docs/tigergraph_foundation/tigergraph/queries/tests/query_cases.json` | M | advisor_id in the two cases |
| `app/api/routers/v2.py` | M | async POSTs + advisor_id params. **CONFLICT-RISK** |
| `scripts/verify_per_advisor.py` | A | 33-check B/C suite (temp data-set copy, LLM mock) |
| `scripts/verify_judge.py` | M | gate-inspection follows `_run_for_advisor` |

## Work-stream B + C frontend (commit 95877ec)

| File | Change | Notes |
|---|---|---|
| `frontend/lib/api/v2.ts` | M | advisor_sid on version/scan types; JobStatus/JobStart; advisor-scoped + async API fns. **CONFLICT-RISK** |
| `frontend/components/patterns/job-progress.tsx` | A | `useAsyncJob` + `JobProgressOverlay` |
| `frontend/app/(dashboard)/ai-insights/page.tsx` | M | per-advisor versions; async regenerate; overlay. **CONFLICT-RISK** |
| `frontend/components/ai-insights/commentary-cards.tsx` | M | two labelled Regenerate buttons. **CONFLICT-RISK** |
| `frontend/app/(dashboard)/anomalies/page.tsx` | M | advisor-scoped screen; two Rescan buttons; overlay |

## Work-stream D + wrap (commits b14799f + wrap)

| File | Change | Notes |
|---|---|---|
| `scripts/generate_sample_data.py` | M | July 2026; anomaly-rule stories; 92/9L; per-scenario comments. **CONFLICT-RISK** |
| `data/sample/**` (58 CSVs) | M | regenerated 4-month set + commentary v22–v24 + scans 003–005 |
| `app/v2/assistant/service.py` | M | figures-payload label-collision fix |
| `scripts/verify_end_to_end.py` | M | per-advisor PUBLISHED checks; residual-demo MIX exemption |
| `scripts/verify_attribution.py` | M | residual-demo MIX exemption |
| `scripts/verify_anomalies.py` | M | --rescan handles per-advisor scan_ids |
| `scripts/verify_assistant.py` | M | uniquified figure keys (mirrors service) |
| `CLAUDE.md` | M | rule 10: sample-data completeness standing principle. **CONFLICT-RISK** |
| `docs/SOLUTION_GUIDE.md` | M | Round-11 standing rule block. **CONFLICT-RISK** |
| `docs/ROUND11_ACCEPTANCE.md` | A | operator-only checks |
| `docs/ROUND11_CHANGED_FILES.md` | A | this file |
| `BUILD_REPORT.md` | M | §17 |
| `PROGRESS.md` | M | P-A1..P-F1 |
| `ROUND11_STARTER_PROMPT.md` | A | operator's round-11 brief (committed for the record) |
